const Config = {
    SIZE: Math.min(Math.floor(window.innerWidth / 20), 20),
    SIZE2: 0,
    QUEUE_MODAL: 20,
    QUEUE_UI: 10,
    QUEUE_NPC: 5,
    QUEUE_PROPS: 0,
}

Config.SIZE2 = Config.SIZE * 2;

export default Config;